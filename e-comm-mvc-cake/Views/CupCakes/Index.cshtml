@model List<CupCake>

@{
	ViewData["Title"] = "List Of CupCakes";
}
<h1>LIST OF CUPCAKES</h1>
<table class="table">
	<thead>
		<tr>
			<td style="font-weight:bold;font-size:40px">Id</td>
			<th style="font-weight:bold;font-size:40px">CupCake Image</th>
			<th style="font-weight:bold;font-size:40px">CupCake Name</th>
			<th style="font-weight:bold;font-size:40px">CupCake Flavour</th>
		</tr>
	</thead>
	<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td style="font-weight:bold;font-size:30px">@item.Id</td>;
					<td><img src="@item.CupCakeImage" alt="CupCakeName" class="rounded-circle"style="max-width:100px" /></td>;
					<td style="font-style:italic;font-size:30px">@item.CupCakeName</td>;
					<td style="font-style:italic;font-size:30px">@item.CupCakeFlavour</td>;
					<td class="align-middle">
						<a class="btn btn-outline-primary"asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>Edit</a>
						<a class="btn btn-outline-info"asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-eye"></i>Details</a>
						<a class="btn btn-danger"asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash"></i>Delete</a>
					</td>
				</tr>				
			}		
	</tbody>
</table>

<div class="text-white">
	<a asp-controller="CupCakes" asp-action="Create" class="btn btn-success"><i class="bi bi-plus-circle"></i>Add New</a>
</div>

